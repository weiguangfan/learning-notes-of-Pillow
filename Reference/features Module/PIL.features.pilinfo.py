"""
PIL.features.pilinfo(out=None, supported_formats=True)
打印关于此Pillow安装的信息。
这个函数可以用python3 -m PIL调用。

PARAMETERS:
    out - 要打印的输出流。
        如果没有，默认为 sys.stdout。

    supported_formats - 如果为真，将打印所有支持的图像文件格式的列表。

"""